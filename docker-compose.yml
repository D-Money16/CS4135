services:

  postgres:
    image: postgres:16
    container_name: postgres_db
    environment:
      POSTGRES_DB: mydb
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U myuser -d mydb"]
      interval: 10s
      timeout: 5s
      retries: 5

  elib-discovery:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        SERVICE_NAME: elib-discovery
    ports:
      - "8761:8761"
    environment:
      SERVER_PORT: 8761

  elib-identity:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        SERVICE_NAME: elib-identity
    ports:
      - "9921:9921"
    depends_on:
      postgres:
        condition: service_healthy
      elib-discovery:
        condition: service_started
    environment:
      SERVER_PORT: 9921
      EUREKA_URI: http://elib-discovery:8761/eureka
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres_db:5432/mydb?currentSchema=identity
      SPRING_DATASOURCE_USERNAME: myuser
      SPRING_DATASOURCE_PASSWORD: mypassword

  elib-catalogue:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        SERVICE_NAME: elib-catalogue
    ports:
      - "9922:9922"
    depends_on:
      postgres:
        condition: service_healthy
      elib-discovery:
        condition: service_started
    environment:
      SERVER_PORT: 9922
      EUREKA_URI: http://elib-discovery:8761/eureka
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres_db:5432/mydb?currentSchema=catalogue
      SPRING_DATASOURCE_USERNAME: myuser
      SPRING_DATASOURCE_PASSWORD: mypassword

  elib-lending:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        SERVICE_NAME: elib-lending
    ports:
      - "9923:9923"
    depends_on:
      postgres:
        condition: service_healthy
      elib-discovery:
        condition: service_started
      elib-catalogue:
        condition: service_started
    environment:
      SERVER_PORT: 9923
      EUREKA_URI: http://elib-discovery:8761/eureka
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres_db:5432/mydb?currentSchema=lending
      SPRING_DATASOURCE_USERNAME: myuser
      SPRING_DATASOURCE_PASSWORD: mypassword

  elib-notification:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        SERVICE_NAME: elib-notification
    ports:
      - "9924:9924"
    depends_on:
      elib-discovery:
        condition: service_started
    environment:
      SERVER_PORT: 9924
      EUREKA_URI: http://elib-discovery:8761/eureka

  elib-bookclub:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        SERVICE_NAME: elib-bookclub
    ports:
      - "9925:9925"
    depends_on:
      elib-discovery:
        condition: service_started
    environment:
      SERVER_PORT: 9925
      EUREKA_URI: http://elib-discovery:8761/eureka

  elib-gateway:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        SERVICE_NAME: elib-gateway
    ports:
      - "9920:9920"
    depends_on:
      - elib-discovery
      - elib-identity
      - elib-catalogue
      - elib-lending
      - elib-notification
      - elib-bookclub
    environment:
      SERVER_PORT: 9920
      EUREKA_URI: http://elib-discovery:8761/eureka

volumes:
  postgres_data:
