spring.application.name=elib-gateway

# Disable Eureka during tests — no discovery server available
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
spring.cloud.discovery.enabled=false

# Disable property-configured routes so only the GatewayConfig bean routes are active
spring.cloud.gateway.mvc.routes=
app.jwt.secret=elib-jwt-key-32bytes!!!!!!!!!!!!!!
